# MyDarts Environment Configuration
# Copy this to .env and fill in your values

# Spotify Configuration (optional - only needed if using Spotify integration)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Autodarts Configuration (optional - can be set via UI instead)
# Leave as localhost:8079 since darts-caller runs on the same device
DARTSCALLER_URL=http://localhost:8079

# Autodarts Credentials (for running darts-caller)
AUTODARTS_EMAIL=your_autodarts_email@example.com
AUTODARTS_PASSWORD=your_autodarts_password
AUTODARTS_BOARD_ID=your_board_id_from_autodarts

# Network Configuration
# The app will automatically detect the correct URLs based on hostname
# No need to configure IPs manually!
